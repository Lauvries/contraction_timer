# Optional: copy to .env and `export $(grep -v '^#' .env | xargs)` before npm run build / deploy:netlify
SUPABASE_URL=
SUPABASE_ANON_KEY=
# NETLIFY_AUTH_TOKEN=   # for CLI: netlify login, or paste token for CI/scripts
